---
swagger: "2.0"
info:
  x-ibm-name: "test1"
  title: "test1"
  version: "1.0.0"
schemes:
- "https"
host: "$(catalog.host)"
basePath: "/test1"
consumes:
- "application/json"
produces:
- "application/json"
securityDefinitions:
  clientIdHeader:
    type: "apiKey"
    in: "header"
    name: "X-IBM-Client-Id"
security:
- clientIdHeader: []
x-ibm-configuration:
  testable: true
  enforced: true
  cors:
    enabled: true
  assembly:
    execute:
    - activity-log:
        title: "activity-log"
        content: "activity"
        error-content: "payload"
        version: "1.0.0"
    - gatewayscript:
        title: "gatewayscript"
        version: "1.0.0"
        source: "some replacement"
  phase: "realized"
  categories:
  - "level1/level2"
paths:
  /reflect:
    get:
      responses:
        200:
          description: "200 OK"
definitions: {}
tags: []
